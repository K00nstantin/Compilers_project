MODULE TestReal;
  CONST pi = 3.14159;
  VAR r: REAL; i: INTEGER; ok: BOOLEAN; result: INTEGER;
BEGIN
  i := 2;
  r := pi * i;     (* 6.28318 *)
  ok := (r > 6.0) & (r < 7.0);
  IF ok THEN result := 0 ELSE result := 1 END
END TestReal.